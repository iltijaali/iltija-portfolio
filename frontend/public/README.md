# Public assets

Save your profile photo here as `profile-photo.jpg` (this exact filename).
The About section (`src/components/About.tsx`) loads it from `/profile-photo.jpg`;
if the file is missing it automatically falls back to an initials avatar, so the
site still works without it.
